import styled from "styled-components";

const RoomChange = styled.button`
  border: none;
  border-radius: 8px;
  margin-left: 10px;
  padding: 5px 10px;
  font-weight: bold;
  &:hover {
    cursor: pointer;
  }
`;
export default RoomChange;
